//return the total number of smiling faces in the array
function countSmileys(arr) {
  
let y= arr.filter((x)=> x===':)' || x===':D' || x=== ';-D' || x===':~)'|| x===':-)'||x ===';~)'||x===':-D'||x===':~D'|| x===';)'||x===';~D'||x===';D'||x===';-)')
return y.length
}
